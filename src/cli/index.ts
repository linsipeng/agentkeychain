#!/usr/bin/env bun
/**
 * agentkeychain CLI entry.
 * Stub for bootstrap — replaced in F-1.
 */
import { VERSION } from "../index.ts";

function main(): void {
  console.log(`agentkeychain v${VERSION}`);
  console.log("(bootstrap stub — implementation coming in F-1)");
}

main();
