name = "agentkeychain-sync"
main = "src/index.ts"
compatibility_date = "2026-08-01"
account_id = "REPLACED_BY_SYNC_INIT"

[[d1_databases]]
binding = "DB"
database_name = "agentkeychain-sync"
database_id = "REPLACED_BY_SYNC_INIT"

# SYNC_TOKEN is injected as a secret by `agentkeychain sync init` (wrangler secret put).
